enum Planets {
	MERCURIO,
	VENUS,
	TERRRA,
	MARTE,
}

enum PlanetsOp2 {
	MERCURIO = 1,
	VENUS,
	TERRRA,
	MARTE,
}

enum Database {
	CREATE = "create",
	DELETE = "delete",
	UPADET = "upadt",
	READ = " read",
}
